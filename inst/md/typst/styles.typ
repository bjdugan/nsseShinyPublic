#let set-text-styles() = {
  set text(
    font: "Source Sans Pro",
    size: 11pt,
    leading: 1.4em,
  )

  set heading(
    font: "Source Sans Pro",
    weight: "semibold",
  )

  set code(
    font: "Source Code Pro",
    size: 9.5pt,
  )
}
